/**
 * WebSocket service barrel export
 */

export { wsService } from './wsService';
export type {
  ServerMessage,
  ServerMessageType,
  WsConnectionState,
  WsEventHandler,
  WorkflowProgressPayload,
  AuthChangedPayload,
} from './types';
